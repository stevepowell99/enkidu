# Local development example (DO NOT commit real secrets here)

# OpenAI
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Storage mode
# - local: uses ./memories (default)
# - supabase: uses Supabase Postgres via REST (requires SUPABASE_* below)
ENKIDU_STORAGE=local

# Supabase (required when ENKIDU_STORAGE=supabase)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=


